{
  "name": "brand-alchemy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Brand-Alchemy — standalone static site (no WordPress). Vite-built JS/CSS/Three.js bundle; deploys to any static host.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-portfolio.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.15"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "lenis": "^1.3.23",
    "postprocessing": "^6.39.1",
    "three": "^0.184.0"
  }
}
